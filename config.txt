# A-maze-ing Configuration File
# Maze dimensions (automatically converted to odd numbers if needed)
WIDTH=6
HEIGHT=9
# Entry and exit coordinates (x,y)
ENTRY=3 ,4
EXIT=5,5
# Output file for hex format maze
OUTPUT_FILE=maze.txt
# Generate perfect maze (single path between entrance and exit)
PERFECT=True
# Optional: Random seed for reproducible mazes
# SEED=42
# Optional: Animation delay in seconds
DELAY=0.05
